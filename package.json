{
  "name": "goplanme-root",
  "version": "1.0.0",
  "description": "Root package for GoPlanMe to run backend and frontend concurrently",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\"",
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm start --prefix backend",
    "frontend": "npm start --prefix frontend",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && rm -f frontend/build/_headers frontend/build/_redirects"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "body-parser": "^2.3.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.7.3",
    "multer": "^2.2.0",
    "nodemon": "^3.1.14",
    "qrcode": "^1.5.4",
    "serverless-http": "^4.0.0",
    "uuid": "^14.0.1"
  }
}
